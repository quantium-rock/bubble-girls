version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --global yarn
        - yarn install
    build:
      commands:
        - yarn run build:$BUILD_ENV
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
        - node_modules/**/*